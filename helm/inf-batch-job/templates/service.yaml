apiVersion: v1
kind: Service
metadata:
  name: {{ include "inf-batch-job.fullname" . }}
  labels:
    {{- include "inf-batch-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inf-batch-job.selectorLabels" . | nindent 4 }}
