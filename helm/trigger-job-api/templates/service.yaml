apiVersion: v1
kind: Service
metadata:
  name: {{ include "trigger-job-api.fullname" . }}
  labels:
    {{- include "trigger-job-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trigger-job-api.selectorLabels" . | nindent 4 }}
